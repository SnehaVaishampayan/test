version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file cfntemplate.yaml --s3-bucket $BUCKET --output-template-file outputtemplate.yaml
artifacts:
  type: zip
  files:
    - template.yaml
    - outputtemplate.yaml